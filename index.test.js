/**
const { default: testRunner } = require("jest-runner");

const getDiscount = require(`./index`);

test('Return discount according to price of lemon bought', () => {
    expect(getDiscount(true, 2)).toBe(
    //   .......
    );
});
*/
